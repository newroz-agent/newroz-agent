#!/usr/bin/env python3
"""
Newroz Agent CLI launcher.

This wrapper should behave like the installed `newroz` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from newroz_cli.main import main
    main()
